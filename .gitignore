results/*.sqlite
__pycache__/
*.pyc
.venv/
*.egg-info/
.env

.pytest_cache/

# React webapp
webapp/node_modules/
webapp/dist/
webapp/.vite/
webapp/out/

# Temporary iconset directories
webapp/build/*.iconset/
build/*.iconset/

# Agent artifacts (plans, local agent state) -- never commit
.hermes/

# Stray secrets -- never commit
signing.env
.signing-keys/
.DS_Store

# CodeGraph artifacts -- regenerated per machine (init --index); never track.
# Tracking .codegraph/.gitignore churned on every machine and blocked ff updates.
.codegraph/
